# 服务器配置
PORT=8080
GIN_MODE=debug

# 数据库配置
DB_HOST=localhost
DB_PORT=5432
DB_NAME=erp_db
DB_USER=postgres
DB_PASSWORD=password
DB_SSLMODE=disable

# JWT配置（后续扩展使用）
JWT_SECRET=your-secret-key
JWT_EXPIRE_HOURS=24 